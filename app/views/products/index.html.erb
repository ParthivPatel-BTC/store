<h1> <%= @category.title %> </h1>
<% @products.each do |product| %>
  <div> <%= link_to product.name, category_product_path(@category.id, product.id)%> </div> 
<% end%>
<br>
<br>
<%= paginate @products %>